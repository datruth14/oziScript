
{
    "name": "ozi/ozi_script",
    "description": "Ozi script Is an easy-to-use web development framework that is built to handle both front and back-end web development. Ozi script is a component-based concept, meaning that everything in ozi script is a component and a component is a method or functions created to handle a specific task.",
    "type": "project",
    "keywords": ["php", "framework", "web"],
    "license": "MIT",
    "authors": [
        {
            "name": "Victor Francis",
            "homepage": "https://github.com/datruth14/oziScript.git",
            "role": "Lead Developer"
        }
    ],
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "phpstan/phpstan": "^1.10"
    },
    "autoload": {
        "psr-4": {
            "OziFramework\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "OziFramework\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "post-create-project-cmd": [
            "@php artisan migrate",
            "@php artisan serve"
        ],
        "test": "phpunit",
        "analyze": "phpstan analyse"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}

